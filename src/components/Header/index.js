import React from 'react';

import './styles.css';

const Header = () => (
  <div className="header">
    <a href="#" className="facebook-logo">facebook</a>
    <a href="#" className="my-profile-link">Meu perfil</a>
  </div>
);

export default Header;
